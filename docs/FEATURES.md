# Описание секций и функций

## Основное приложение (`index.html` в корне)

### Header (Шапка)

- Логотип `logo.png` с инверсией цвета (фильтр `invert`)
- Название `HUSAM` в фирменном стиле
- Навигация-якоря: `#услуги`, `#процесс`, `#контакты`
- Кнопка звонка/мессенджер с номером `+7 (928) 945-31-31`
- Адаптивный бургер для мобильных устройств
- При прокрутке > 50px добавляется фон с blur-эффектом

### Hero‑блок

- Фоновое изображение `image.jpg` + затемняющая подложка и градиент
- Заголовок «Строим и сдаем под ключ, качественно и в срок»
- Описание и две основные кнопки:
  - «Рассчитать смету» — прокручивает к квизу
  - «Оставить заявку» — прокручивает к контактам

### Почему выбирают HUSAM (преимущества)

Грид карточек с иконками lucide:

1. **Полный цикл работ** — от проектирования до сдачи объекта
2. **Сервисное обслуживание** — официальное сервисное обслуживание на выполненные работы
3. **Профессиональная команда** — опытные специалисты с многолетним стажем работы
4. **Прозрачная отчетность** — детальная отчетность по выполненным работам и закупленным материалам
5. **Сроки под контролем** — четкое планирование этапов
6. **Постоянная связь** — регулярные отчеты о ходе работ
7. **Сметы и КП** — возможность составления смет и коммерческих предложений при наличии проекта

### Услуги (`id="услуги"`)

4 карточки с услугами:

1. **Проектирование**
   - Архитектурный проект
   - Конструктивные решения
   - Инженерные системы
   - 3D-визуализация

2. **Дизайн**
   - Дизайн-проект
   - Подбор материалов
   - Авторский надзор
   - Комплектация объекта

3. **Строительство**
   - Фундаментные работы
   - Возведение стен
   - Кровельные работы
   - Фасадные работы

4. **Ремонт**
   - Черновая отделка
   - Чистовая отделка
   - Электромонтаж
   - Сантехнические работы

Для каждой услуги есть кнопка «Узнать стоимость», которая открывает мессенджер с указанием выбранной услуги.

### Наши результаты (count-up)

Блок статистики с анимацией чисел при появлении в viewport:

- **9+** лет на рынке
- **250+** сданных объектов
- **45000+** м² отремонтировано
- **60+** постоянных партнеров

Анимация реализована через `useCountUp()` с использованием `requestAnimationFrame` и easing-функции.

### Каталог и готовые проекты

- Публичная страница проектов показывает обычные проекты и готовые проекты на продажу с общей пагинацией и фильтрами по типу, площади, комнатам, этажности и материалу.
- Карточки каталога используют общий визуальный вариант `Card variant="listing"`; визуальный skin карточки хранится в UI-компоненте, а страницы управляют только составом и размещением.
- Для готовых проектов карточка показывает компактный набор характеристик: площадь дома, этажность, спальни/комнаты, стены, стиль, участок и заполненные пользовательские поля в пределах компактного лимита.
- Детальная страница готового проекта выводит:
  - галерею изображений;
  - описание;
  - поэтажную экспликацию отдельными блоками (`Подвал`, `1 этаж`, `2 этаж`), если она импортирована структурированно;
  - fallback на старую текстовую экспликацию, если структурированных данных нет;
  - отдельный блок «Конструктив и материалы» для фундамента, стен, кровли, фасада и общего описания материалов;
  - характеристики и цены проекта.

### Админка и импорт готовых проектов

- В админке можно создавать, редактировать, публиковать, скрывать и сортировать проекты и готовые проекты.
- Изображения в формах проектов можно перетаскивать, удалять и назначать главным фото; при дозагрузке новых файлов порядок сохраняется по именам, которые вернул PocketBase после `images+`.
- Массовый импорт готовых проектов работает через CSV-шаблон на основе реестра:
  - артикул, название, стиль, описание, размеры дома;
  - экспликация подвала, 1 и 2 этажей;
  - гараж, навес, терраса, подвал, спальни, этажность;
  - тип фундамента, стены, кровля, облицовка фасада, общее описание материалов;
  - площади, стоимость проекта, скидка, цена со скидкой, цена распечатки, статус сайта;
  - ссылки на фотографии и примечание.
- URL фотографий из CSV сохраняются как legacy-ссылки для последующей миграции файлов; сам CSV-импорт не загружает удалённые изображения в file-поле PocketBase.
- При ручном редактировании импортированного готового проекта структурированные данные не затираются без причины: поэтажная экспликация сохраняется, если админ не менял общий текст экспликации, а материалы/признаки синхронизируются с актуальными полями формы.

### Квиз расчета

Квиз ведет пользователя по шагам и формирует текст заявки:

- выбор услуги: Строительство / Ремонт / Дизайн / Проектирование;
- выбор объекта или направления;
- наличие проекта для строительства;
- бюджет или тип объекта для проектирования;
- имя, телефон и отдельное согласие на обработку персональных данных.

**Клиентская валидация:**

- обязательность выбора на каждом шаге;
- имя не короче 2 символов;
- отдельная проверка телефона по `phonePattern`;
- отправка финального шага блокируется без согласия;
- ошибки подсвечиваются через `.form-error` и текстовые сообщения.

При успешной валидации формируется текст заявки, добавляются источник/UTM-метки, отправляется цель Метрики и открывается мессенджер.

### Процесс работы (`id="процесс"`)

6 шагов работы, оформлено карточками с крупным номером шага:

1. **Бриф** — обсуждаем ваши пожелания, бюджет и сроки
2. **Замер** — выезд специалиста на объект для точных измерений
3. **Смета** — детальный расчет стоимости всех работ и материалов
4. **Договор** — заключение договора с фиксацией сроков и цен
5. **Работа** — выполнение работ с регулярными отчетами
6. **Сдача** — приемка объекта и передача гарантийных документов

### FAQ

Список вопросов/ответов, реализован через `AccordionItem` с локальным `open`‑состоянием:

1. Какие сроки выполнения работ?
2. Какие гарантии вы предоставляете?
3. Как формируется смета?
4. Какие материалы вы используете?
5. Как происходит оплата?
6. Можно ли вносить изменения в процессе работы?

### Контакты (`id="контакты"`)

**Левая колонка** — контактная информация:

- **Адрес**: г. Грозный, пр-т Кадырова, 274
- **Телефон**: +7 (928) 945-31-31
- **Email**: husamstroy_2020@mail.ru
- **Сайт**: husam.ru

**Правая колонка** — форма «Оставьте заявку»:

- Имя (обязательное)
- Телефон (обязательное, с валидацией)
- Тип услуги (селект, обязательное)
- Комментарий (опциональное)
- Чекбокс согласия с ссылками на `/consent` и `/privacy`

Валидация (обязательные поля + паттерн телефона). Отправка — формирование текстового запроса и переход в мессенджер.

### Юридические страницы

- `/privacy` — политика в отношении обработки персональных данных.
- `/consent` — согласие на обработку персональных данных.
- Ссылки на документы размещены в футере, форме контактов и квизе.
- Отправка формы контактов и финального шага квиза требует отдельного подтверждения согласия.

### Аналитика и источники заявок

- При наличии `VITE_YANDEX_METRIKA_ID` сайт инициализирует Яндекс.Метрику.
- Отправляются цели: `click_phone`, `click_messenger`, `contact_form_submit`, `quiz_submit`, `project_open`, `project_cta_click`.
- В текст заявки автоматически добавляется контекст источника: текущий URL, заголовок страницы, форма/проект и UTM-метки.

### Footer

- Логотип и краткое описание компании
- Список ссылок-якорей по секциям
- Блок «Документы» со ссылками на `/privacy` и `/consent`
- Нижняя плашка с копирайтом и ссылкой на политику обработки персональных данных
